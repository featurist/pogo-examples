person = {

  name = 'Man Ray'

  say hi later() =
    set timeout
      console. log "my name is #(self.name)"
    123

}

not using an arrow
  means (self) is not always (this)

using an arrow =>
  means (self) is always (this)